version: "3.9"

services:
  landing-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - HOST=0.0.0.0
      - PORT=8080
      - RUST_LOG=info
      - CORS_ORIGIN=http://localhost:8080
    restart: unless-stopped
